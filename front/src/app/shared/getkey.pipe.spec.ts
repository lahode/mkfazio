/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { GetkeyPipe } from './getkey.pipe';

describe('Pipe: Getkey', () => {
  it('create an instance', () => {
    let pipe = new GetkeyPipe();
    expect(pipe).toBeTruthy();
  });
});
